{{content}}
<p class="weather-notice">Fetching Weather...</p>

<form class="weather-form">
  <div>
    <label for="weather-input">Location (US only)</label>
    <input
      type="search"
      id="weather-input"
      name="weather-input"
      placeholder="e.g. Boston"
    />
  </div>
  <button type="submit">Fetch Weather</button>
</form>